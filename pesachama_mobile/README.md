# Legacy Mobile Folder

`pesachama_mobile/` is not the active Flutter application.

The maintained mobile client lives in `ratibu_mobile/`. This directory only
contains leftover Android artifacts from an earlier structure and should not be
used for new feature work.
